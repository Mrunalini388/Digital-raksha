services:
  - type: web
    name: digital-raksha
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python -m spacy download en_core_web_sm
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10


